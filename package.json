{
  "name": "jixels-id-cards",
  "version": "1.0.0",
  "private": true,
  "description": "Jixels ID card registration and approval app.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check tests/server.test.js && node --check sw.js",
    "test": "node --test"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "qrcode": "^1.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
